// Rutas para gestionar avisos de empleo (Conecta con los controllers)

import express from 'express'
const router = express.Router()

// Aquí luego agregarás tus rutas, por ejemplo:
// router.get('/', ...)
// router.post('/', ...)



export default router